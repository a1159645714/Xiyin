# 希音平台全自动上品工具开发进度

更新时间：2026-08-04（架构说明已同步至 v1.0.7）

## 项目概况

本项目是一个基于 Python、PyQt5、Playwright 的希音平台自动上品工具。通过图形界面选择参考图搜索机会商品、勾选商品后自动生成图片与标题，并驱动浏览器执行商品发布流程。

## 当前代码结构

- `main.py`：程序入口，仅负责启动桌面界面。
- `config.py`：页面文案、默认值、路径、版本号和业务常量。
- `product_files.py`：商品目录扫描、图片/视频定位、`商品配置.json` 读取。
- `config_services.py`：商品数据解析、价格换算与轮次配置写入。
- `image_services.py`：AI 九宫格生成、裁剪与图片下载。
- `ai_content_service.py`：对话模型标题生成与标题规范化。
- `协议AI.py`：AI 服务配置与九宫格提示词。
- `aiprice_image_search.py`：AliPrice 图片搜索接口复现，支持 16 个平台。
- `category_catalog.py`：本地类目树的读取、保存和路径校验。
- `store_profiles.py`：玩具店、家居店两套店铺画像（类目树与发布类目）。
- `real_photo_library.py`：实拍图库扫描，支持商品直存与颜色/款式变体目录。
- `automation.py`：Playwright 浏览器操作和完整发布流程。
- `task_workers.py`：后台线程、AI 生成流水线与发布编排。
- `unified_gui.py`：PyQt5 Fluent 界面、设置持久化、运行日志和线程控制。
- `update_service.py`：启动时检查更新、下载 ZIP、校验 SHA-256，并启动更新器。
- `updater_main.py`：在主程序退出后替换文件并重新启动新版本。
- `build_windows.bat`、`build_release.py`、`upload_release.py`：按版本生成热更新包并上传腾讯云 COS。

拆分以保持原有流程行为不变为目标，后续页面定位稳定性和停止机制会在独立迭代中继续优化。

## 运行可靠性改进

- 停止按钮改为协作式取消：当前页面操作结束后由浏览器工作线程自行清理 Chrome 和临时文件，避免界面线程与 Playwright 同时操作；正常发布完成后会保留浏览器，方便人工核对结果。
- 启动前会预检商品配置、供方货号、数字图片目录、本体实拍图和包装实拍图，缺少资料时不进入发布页面。
- 数字图片目录中没有图片的目录会自动跳过，避免随机选中空目录后中断。
- 已添加 `.gitignore`，避免提交浏览器登录态、Cookie、个人设置、虚拟环境和缓存。
- GUI 支持选择稀缺爆款、平台热卖、潜力趋势，并支持按平台同步的任意层级类目逐级选择。
- 稀缺爆款会自动选择其下的“市场爆款”；三个机会页签随后共用同一条发布流程。
- 程序窗口标题会显示当前版本号，例如 `希音平台全自动上品工具 v1.0.2`，便于确认是否已更新完成。

## 热更新与自动发布

- 已接入腾讯云 COS 热更新。程序启动约 1 秒后会读取线上 `update.json`；线上版本高于本地 `APP_VERSION` 时，会弹出更新提示。
- 更新包下载后会校验 SHA-256。校验通过后由独立的 `updater.exe` 在主程序退出后替换文件并自动重启。
- `updater.exe` 不是用户操作的第二个程序，而是用于替换正在运行的主程序；交付时必须保留在应用目录中。
- 已完成从 `1.0.0` 测试版本升级到 `1.0.1` 的完整验证，包括线上清单读取、ZIP 下载、哈希校验、替换和重启。
- `build_windows.bat` 现已集成自动发布：打包成功后会自动读取 `config.py` 中的 `APP_VERSION`，生成更新 ZIP 与 `update.json`，再按 ZIP、清单的顺序自动上传至 COS。
- `发布热更新包.bat` 也支持独立执行，同样自动读取 `APP_VERSION`，无需手动输入版本号。
- COS 凭证只保存在本机 `cos_credentials.json`，并已加入 `.gitignore`；该文件不会进入应用发布包或热更新 ZIP。
- `update.json` 使用无 BOM 的 UTF-8 编码，避免 Windows PowerShell 写入 BOM 后导致程序无法解析更新清单。

发布新版本时只需：

1. 在 `config.py` 中提高 `APP_VERSION`。
2. 双击 `build_windows.bat`。
3. 等待打包和 COS 上传全部完成。

## 已完成核心功能

### 浏览器与登录

- 使用 Playwright 启动 Chrome 持久化浏览器环境。
- 支持加载 `cookies.json` 复用登录状态。
- 支持用户在界面设置浏览器窗口尺寸，默认按当前屏幕自适应。
- 浏览器操作已加入全局自动滚动，点击、填写、选择、上传前会尽量滚动到目标区域，方便观察问题。

### 图形操作界面

- 界面已从 Tkinter 迁移到 PyQt5 + PyQt-Fluent-Widgets，包含「选品搜图 / AI设置 / 上架设置 / 执行中心」四个页面。
- 支持选择参考图与目标平台搜图、选择商品总目录、Cookie 文件。
- 支持设置浏览器窗口宽高。
- 支持持久化保存界面设置，下次打开自动恢复。
- 支持运行过程中手动暂停和继续。
- 支持流程运行日志显示。

### 商品目录与配置读取

- 用户只需要选择一个商品总目录。
- 商品图片默认从总目录下的数字子目录中随机选择，例如 `1`、`2`、`3`、`4`。
- 商品轮播图优先选择文件名包含“主图”的图片；如果没有“主图”文件，则默认选择该数字子目录中的第一张图片。
- 商品视频默认读取商品总目录下第一个 `.mp4` 文件。
- 商品标题、必填属性、包装信息、供方信息、颜色配置、证书列表等均从商品总目录下的 `商品配置.json` 读取。
- 供方货号和证书列表已改为从 JSON 读取，不再依赖文件夹名称。

### 图片处理

- 已接入 Pillow。
- 所有图片上传前都会转换为 1:1 图片。
- 转换后的图片存放在临时目录中。
- 流程结束后清理临时目录。

### 前置选品流程

- 自动打开目标页面。
- 点击“市场爆款”。
- 选择类目：`玩具&游戏 > 儿童艺术和手工艺`。
- “玩具&游戏”会点击两次，避免第一次点击变成全选。
- 点击“搜索”。
- 在列表中随机点击一个“发布同款”。
- 处理发布页新标签页。
- 选择发布类目：
  - 儿童手工套装
  - 儿童时尚手工艺套件
- 点击“确认，下一步”。
- 处理“我知道了”、“新版图片上传”、“确定”等提示。

### 商品图片与视频

- 上传商品轮播图。
- 上传细节图：上传随机数字图片目录中的全部图片。
- 上传商品视频：从商品总目录读取 `.mp4` 文件。
- 去掉了旧的图片裁剪流程，改为上传前本地转换 1:1。

### 商品标题与必填属性

- 商品标题从 `商品配置.json` 读取并填写。
- 必填属性从 JSON 读取并填写：
  - 敏感类别
  - 适用年龄
  - 关税种类
  - 材质
  - 成分
  - 成分比例
- 下拉框选择采用“输入后等待，再按回车”的方式，提升稳定性。

### 规格模板与颜色配置

- 自动点击“使用模板”。
- 点击“使用模板”后，根据 `商品配置.json` 中的 `颜色配置` 填写颜色。
- 第一个颜色替换默认的“均码”。
- 后续颜色依次填写到新的“请选择或自定义”输入框。
- 每次颜色输入后会让输入框失焦。
- 使用模板和颜色填写完成后，流程会直接继续上传细节图和填写后续信息。

### 供方信息

- 价格从 JSON 配置的价格范围中随机生成一次。
- 多个颜色行共用同一个随机价格。
- 每个颜色行都会填写相同价格。
- 每个颜色行都会点击“编辑库存”并填写库存。
- 每个颜色行都会选择件数类型。
- 供方货号从 JSON 中读取并填写。
- 商品 SKC 从供方信息表格中读取，后续用于产品标识符。

### 包装信息

- 包装信息从 JSON 读取。
- 支持多颜色多行。
- 每个颜色行都会填写同样的包装参数：
  - 含包装重量
  - 长
  - 宽
  - 高
  - 单位
  - 包装类型
- 定位方式已按行号 class 处理，避免多颜色合并单元格导致列错位。

### 资质与证书上传

- 支持上传玩具说明书。
- 支持上传以下证书：
  - ASTM F963报告
  - CPC证书
  - 玩具EN 71检测报告
  - REACH二甲酸酯含量
  - 欧洲玩具检测报告:EN71-1/2/3
  - TOY自符声明-EU
  - CPSIA报告
  - 韩国安全检测报告（儿童产品）
  - TOY自符声明-UK（手动）
  - POPs化学测试报告
  - PPWR 包装化学测试报告
  - REACH检测报告
  - 技术文件
- 证书上传改为从 JSON 的 `证书列表` 逐项读取；除玩具说明书外，对应证书后方填了证书名才上传，空值会跳过。
- 玩具说明书如果为空值，会沿用旧逻辑默认上传弹窗第一行。
- 上传后会选择适用范围“多色”并提交。
- 上传完成后会点击“不再上传”。
- 证书上传顺序按 JSON 支持列表的固定顺序执行。

### 实拍图上传

- 已改为按文件夹上传，不再按文件名匹配。
- 本体实拍图上传商品总目录下 `本体` 文件夹中的所有图片。
- 包装实拍图上传商品总目录下 `包装` 文件夹中的所有图片。
- 文件夹内图片名称不再重要，只要是 `.jpg`、`.jpeg`、`.png`。
- 上传前仍会统一转换为 1:1 临时图片。
- 上传后提交并点击“不再上传”。

### 合规信息编辑

- 欧盟玩具安全指令：
  - 适用规格：多色
  - 玩具类型：其他
  - 不适用年龄：3岁以下
- 产品标识符：
  - 适用规格：多色
  - 产品标识符填写从供方信息表格读取到的 SKC
- 美国玩具窒息危险提示：
  - 适用规格：多色
  - 玩具危险性描述：该玩具不含上述情况，无需展示窒息危险警告语
- 所有弹窗提交后会处理“不再上传”。

### 发布

- 流程最后点击“发布商品并报名”。

## 当前 JSON 配置约定

商品总目录下需要有 `商品配置.json`，目前使用的关键字段包括：

```json
{
  "商品标题": "...",
  "必填属性": {
    "敏感类别": "其他",
    "适用年龄": "3岁以上",
    "关税种类": "解压玩具",
    "材质": "TPR",
    "成分": {
      "值": "PU",
      "比例": "100"
    }
  },
  "包装信息": {
    "含包装重量(g)": "130",
    "含包装尺寸": {
      "长": "6",
      "宽": "6",
      "高": "6"
    },
    "单位": "cm",
    "包装类型": "软包装+硬物"
  },
  "供方信息": {
    "价格范围": {
      "最低": 50,
      "最高": 100
    },
    "库存": "5000",
    "件数": "单品",
    "供方货号": "西瓜"
  },
  "颜色配置": ["粉红色", "白色", "蓝色"],
  "证书列表": {
    "是否启用": true,
    "玩具说明书": "西瓜",
    "ASTM F963报告": "西瓜",
    "CPC证书": "西瓜",
    "玩具EN 71检测报告": "西瓜",
    "REACH二甲酸酯含量": "西瓜",
    "欧洲玩具检测报告:EN71-1/2/3": "西瓜",
    "TOY自符声明-EU": "西瓜",
    "CPSIA报告": "西瓜",
    "韩国安全检测报告（儿童产品）": "",
    "TOY自符声明-UK（手动）": "",
    "POPs化学测试报告": "",
    "PPWR 包装化学测试报告": "",
    "REACH检测报告": "",
    "技术文件": ""
  }
}
```

`证书列表.是否启用` 默认为 `true`。设为 `false` 时，程序会跳过逐项证书上传；实拍图和合规信息仍会继续处理。

## 当前商品目录约定

用户选择的商品总目录建议包含：

```text
商品总目录/
  商品配置.json
  商品视频.mp4 或任意 .mp4 文件
  1/
    主图.jpeg 或任意图片
    其他细节图...
  2/
    图片...
  本体/
    任意图片...
  包装/
    任意图片...
```

## 后续迭代（v1.0.7 时期）

- 界面迁移到 PyQt5 + PyQt-Fluent-Widgets，新增 AI 设置页；支持配置图像/对话两套 AI 服务并拉取模型列表。
- 新增以图搜品流程：复现 AliPrice 4.0.7 接口，支持 1688、AliExpress、Alibaba、Amazon、Shopee、TikTok 等 16 个平台，结果列表支持勾选与缩略图预览。
- 新增 AI 商品素材生成：九宫格拼图按 3x3 裁剪为 9 张详情图；提示词强调美甲女性手、儿童手、亲子手场景，禁止添加包装、警示标签、认证标志与尺寸参照物。
- 新增「AI生成商品图片」开关：关闭后跳过九宫格生图，恢复早期版本的直传模式——详情图直接上传实拍图库「产品图」文件夹全部图片（文件名含“主图”的优先作为主图），视频取变体目录 mp4，标题仍可单独用对话模型生成。
- 新增标题生成：对话模型按商品名与九宫格提示词规则生成标题，并统一规范为 5PCS–9PCS 开头；AI 失败自动重试而不回退。
- 新增实拍图库：扫描「本体 / 包装 / 产品图 / 视频」目录，支持商品直存与颜色/款式变体两级结构；本体、包装按文件夹批量上传。
- 新增店铺画像：玩具店与家居店分别使用独立的类目树与发布类目，机会商品页支持稀缺爆款、平台热卖、潜力趋势三个页签。
- 新增多轮发布与后台生成：首个商品先生成、后续商品在上架过程中后台串行生成，支持多轮重复上架并隔离输出目录（`output/round_xxx/`）。
- 热更新策略升级：支持 `minimum_supported_version`、`disabled_versions`、`force_update`、`message` 字段；客户端每次启动必须在线确认版本状态，断网时使用缓存清单兜底。
- 适配平台页面改版：供方货号输入框从供方信息表迁移到「规格信息 → SKC列表」后，货号输入框改为全页定位（新旧位置兼容），SKC 从货号输入框所在行读取并兼容两种单元格格式。
- 适配新版细节图表格：`detail_img_container` 改为 颜色/细节图/方形图/色块图 四列，上传入口按表头「细节图」定位左侧列（右侧方形图列不支持批量、多张会提示“不能超过一张”）；改为点击该列「点击上传」控件打开系统文件选择器，每次只选一张、页面自动累积到下一个槽位，并移除所有旧版回退定位。
- 拆分工程结构：浏览器操作、配置读取、图片处理、GUI、后台线程均已拆分为独立模块。

## 当前待优化点

- 部分平台下拉框比较特殊，后续遇到新弹窗时可能还需要针对性优化。
- 证书区域和合规区域依赖页面动态加载，仍需要在真实运行中继续观察稳定性。
- 实拍图上传框定位和上传后的成功判断可以继续优化。
- `商品配置.json` 后续可以继续扩展更多字段，让更多页面参数完全配置化。
- 核心自动化流程（`automation.py`）目前缺少自动化测试，可为重点纯逻辑补充单元测试。
